import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test placeholder', (WidgetTester tester) async {
    // Firebase requires a real device/emulator. Integration tests go in integration_test/.
    expect(true, isTrue);
  });
}
